# Review Guide
## Configuration changes
When a PR modifies configuration loading, include in your review summary the current effective values from the referenced configuration files verbatim so reviewers can confirm the deployment configuration.